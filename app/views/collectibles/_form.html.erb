<%= form_for(@collectible) do |f| %>
  <% if @collectible.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@collectible.errors.count, "error") %> prohibited this collectible from being saved:</h2>

      <ul>
      <% @collectible.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :obj_url %><br />
    <%= f.text_field :obj_url %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :status %><br />
    <%= f.number_field :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
